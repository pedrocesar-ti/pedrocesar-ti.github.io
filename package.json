{
	"scripts": {
		"docs:setup": "npm install gitbook-cli",
		"docs:prepare": "gitbook install",
		"docs:watch": "npm run docs:setup && npm run docs:prepare && gitbook serve",
		"docs:build": "npm run docs:prepare && rm -rf _book && gitbook build"

	}
}
